server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback — react-router-dom handles client-side routing, so a
  # direct/refresh request to e.g. /dashboard must still serve index.html.
  location / {
    try_files $uri /index.html;
  }
}
